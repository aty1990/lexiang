define(['$'], function ($) {

  /*$.fn['initTabs'] = function (options) {
    $(this).on('click', function () {
      var $this = $(this),
          name = $this.data('tabName'),
          $other = $this.siblings('.tab-item-on'),
          otherName = $other.data('tabName');

      // 更改图标样式
      $this.find('.app-icon').removeClass(name).addClass(name + '-selected');
      $this.addClass('tab-item-on').find('div').removeClass('font-gray').addClass('font-red');

      $other.find('.app-icon').removeClass(otherName + '-selected').addClass(otherName);
      $other.removeClass('tab-item-on').find('div').removeClass('font-red').addClass('font-gray');

      options.selectTab(name);
    });
  };*/

});